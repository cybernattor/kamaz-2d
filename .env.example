# Optional public URL used by integrations that need to link back to the game.
APP_URL="http://localhost:3001"

# Renderer selection for local comparison. Pixi/WebGL is the default.
# Set to "canvas" to force the compatibility renderer.
# VITE_RENDERER="pixi"
